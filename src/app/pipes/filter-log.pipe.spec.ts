import { FilterLogPipe } from './filter-log.pipe';

describe('FilterLogPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLogPipe();
    expect(pipe).toBeTruthy();
  });
});
